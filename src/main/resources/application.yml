spring:
  application:
    name: 2025capstone

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/capstone?useSSL=false&useUnicode=true&serverTimezone=Asia/Seoul
    username: root # 나중에 실제 환경에서 변경해주시면 됩니다.
    password: 0IzRxaAlqqxNCYg # 나중에 실제 환경에서 변경해주시면 됩니다.

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

  resources:
    static-locations: classpath:/static/
    add-mappings: true
    cache:
      period: 0

server:
  port: 8080

security:
  jwt:
    token:
      secret-key: authlakdjfalskdfjlaksdfjlkasdnfalskdfnlakqwiuerbwqieufbasidubf
      expire-length : 7200

app:
  upload:
    dir: ${user.home}/Desktop
    image : ${user.home}/Desktop/uploads/images
    video : ${user.home}/Desktop/uploads/videos
    audio : ${user.home}/Desktop/uploads/audios

